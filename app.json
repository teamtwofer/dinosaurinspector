{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "npm install",
        "test": "npm run lint && npm run test"
      },
      "addons": ["heroku-postgresql:hobby-dev"]
    }
  }
}
