{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "npm install",
        "test": "npm run lint && npm run test"
      },
      "addons": ["heroku-postgres:hobby-dev"]
    }
  }
}
